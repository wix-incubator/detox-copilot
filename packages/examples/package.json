{
  "name": "@pilot/examples",
  "description": "Examples usage of pilot and copilot with different framework drivers",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo no build script for this packages",
    "test": "jest",
    "lint": "echo 'No linting configured for this package.'",
    "lint:fix": "echo 'No lint:fix configured for this package.'",
    "type-check": "tsc --noEmit"
  },

  "dependencies": {
     "@pilot/core": "*",
     "@pilot/playwright-driver": "*"
  },

  "devDependencies": {
    "axios": "^1.7.9",
    "playwright": "^1.42.1",
    "puppeteer": "^20.8.0",
    "@playwright/test": "^1.42.1"
  }
} 
